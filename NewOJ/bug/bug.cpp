#include <bits/stdc++.h>

using namespace std;

int n;

int main(){
  freopen("bug.in", "r", stdin);
  freopen("bug.out", "w", stdout);

  cin >> n;
  
  fclose(stdin);
  fclose(stdout);
  return 0;
}