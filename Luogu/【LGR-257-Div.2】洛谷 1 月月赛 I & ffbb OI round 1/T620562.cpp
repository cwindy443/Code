#include <iostream>

int T;

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  std::cin >> T;
  while(T--) {
    int n, m, k;
    std::cin >> n >> m >> k;
    if(n == 1) {
      std::cout << 0 << std::endl;
      continue;
    }
    else {
    }
  }
  return 0;
}