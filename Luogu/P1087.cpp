#include <bits/stdc++.h>

using std::cin;
using std::cout;
using std::endl;

std::string s;
int n;

char dfs(int l, int r){
  char ch;
  if(l == r){
    ch =  
    
  }
  return ch;
}

int main(){
  cin.tie(0);
  cin.sync_with_stdio(NULL);
  cin >> n >> s;
  int size = pow(2, n);
  dfs(0, size - 1);
  return 0;
}