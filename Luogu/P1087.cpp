#include <bits/stdc++.h>

using namespace std;

string s;
int n;

char dfs(int l, int r){
  char ch;
  if(l == r){
    ch =  
  }
  return ch;
}

int main(){
  cin >> n >> s;
  int size = pow(2, n);
  dfs(0, size - 1);
  return 0;
}