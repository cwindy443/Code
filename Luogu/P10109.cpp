#include <iostream>

const int N = 305;
int n, q;
int f[N];

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  std::cin >> n;
  for (int i = 1; i <= n; i++) {
    std::cin >> f[i];
  }
  std::cin >> q; 
  for (int i = 0; i < q; i++) {
    
  }
  
 
  return 0;
}